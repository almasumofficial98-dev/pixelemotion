[project]
name = "pixelemotion"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"git add .
git commit -m "Bumped Python version to 3.12 for Vercel"
git push origin main
dependencies = [
    "flask>=3.1.3",
    "gunicorn>=25.1.0",
]
